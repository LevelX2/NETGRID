{
  "name": "netrunner-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "lint": "corepack pnpm -r --if-present lint",
    "typecheck": "corepack pnpm -r --if-present typecheck",
    "test": "corepack pnpm -r --if-present test && vitest run tests/specs/phase1-artifacts.test.ts --passWithNoTests",
    "build": "corepack pnpm -r --if-present build",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/node": "24.12.2",
    "prettier": "3.8.3",
    "typescript": "6.0.3",
    "vitest": "4.1.5"
  }
}
