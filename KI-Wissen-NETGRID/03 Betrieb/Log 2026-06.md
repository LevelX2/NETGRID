# Log 2026-06

Sortierung: Neueste Einträge stehen oben; ältere Einträge folgen darunter. Dieses Archiv enthält die Logeinträge aus Juni 2026.

## [2026-06-01] startskript | Dev-Server-Restart räumt Prozessbaum

Der Multiplayer-/Netcode-Server war im `dev-watch`-Startpfad blockiert: `scripts/start-netgrid-dev.ps1` stoppte beim Neustart nur den Listener auf Port `8787`, ließ aber alte `corepack`-/`pnpm`-/`tsx watch`-Wrapper ohne aktiven Serverprozess stehen. Danach kam `/health` nicht mehr hoch, obwohl der direkte normale Serverstart funktionsfähig war.

`scripts/start-netgrid.ps1` räumt beim Server-Restart jetzt passende NETGRID-Server-Prozessbäume, bevor ein neuer Server gestartet wird. `scripts/stop-netgrid.ps1` räumt zusätzlich NETGRID-Server- und Web-Prozessbäume, damit Stop/Restart keine toten Wrapper zurücklassen. Verifiziert wurden der direkte Serverstart, `scripts/start-netgrid-dev.ps1`, `/health` auf `127.0.0.1:8787`, PowerShell-Syntax, `git diff --check`, Server-Typecheck und Server-Tests.
