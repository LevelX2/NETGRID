# Classic CardImplementations

Classic implementations live under this directory as concrete card files. CLASSIC-01 intentionally keeps this directory without promoted card files; later CLASSIC packages add one file per implemented Classic card and register it through `subregistries/classic-card-implementations.ts`.
