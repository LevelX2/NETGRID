import { describe, it } from "vitest";

describe("CS11 compiler-gap removal: match FD7671 hint contracts", () => {
  it.todo(
    "reintroduce Rex run-lock and action-projection witnesses only from canonical typed compiler evidence",
  );
});
